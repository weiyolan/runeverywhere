# CLAUDE.md

Guidance for agents working in this repo.

## Agent skills

### Issue tracker

Issues and PRDs are tracked in **GitHub Issues** (`weiyolan/runeverywhere`), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

**Single-context** — `CONTEXT.md` + `docs/adr/` at the repo root (created lazily when terms/decisions get resolved). See `docs/agents/domain.md`.
